\* ================================================================
\* TLC Configuration for StateTransitionCircuit Model Check
\* ================================================================
\*
\* Run manually:
\*   java -cp tla2tools.jar tlc2.TLC MC_StateTransitionCircuit -workers auto
\*
\* Expected: PASS on all 4 invariants across the reachable state space.
\* The key verification target is StateRootChain: that chained multi-tx
\* WalkUp agrees with full-rebuild ComputeRoot at every reachable state.

SPECIFICATION Spec

CONSTANT
    Enterprises <- MC_Enterprises
    Keys <- MC_Keys
    Values <- MC_Values
    DEPTH <- MC_DEPTH
    MaxBatchSize <- MC_MaxBatchSize

INVARIANT
    TypeOK
    StateRootChain
    BatchIntegrity
    ProofSoundness
