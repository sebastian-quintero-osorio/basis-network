{
  "name": "smt-experiment",
  "version": "1.0.0",
  "description": "RU-V1: Sparse Merkle Tree with Poseidon Hash -- Benchmark Experiment",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "benchmark:smt": "node --max-old-space-size=4096 dist/smt-benchmark.js",
    "benchmark:hash": "node dist/hash-comparison.js",
    "benchmark:all": "npm run build && npm run benchmark:hash && npm run benchmark:smt"
  },
  "dependencies": {
    "circomlibjs": "0.1.7",
    "snarkjs": "0.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
