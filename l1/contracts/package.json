{
  "name": "basis-network-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for Basis Network — Avalanche L1 enterprise infrastructure",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.ts --network basisLocal",
    "deploy:fuji": "hardhat run scripts/deploy.ts --network basisFuji",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.19.0"
  }
}
